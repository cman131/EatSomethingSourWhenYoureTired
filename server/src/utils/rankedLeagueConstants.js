// Shared by rankedLeagueService and pointsService (which rankedLeagueService depends on), so it lives apart from both.
const RANKED_GAMES_THRESHOLD = 3;

module.exports = { RANKED_GAMES_THRESHOLD };
